

object Test {
  
  def main( args : Array[String] ) : Unit = {
    Console.println("test...")
    for( i <- 0 until 2000 ){
      for( j <- 0 until 2000 ){
        
      }
    }
    Console.println("done...")
  }
  
}